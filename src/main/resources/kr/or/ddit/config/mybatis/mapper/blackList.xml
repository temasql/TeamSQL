<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blackList">

	<!-- Insert -->
	<insert id="insert" parameterType="blackListVo">
		INSERT INTO BLACKLIST
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="blackListVo">
		SELECT *
		FROM BLACKLIST
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="blackListVo">
		SELECT *
		FROM BLACKLIST
	</select>
	
	<select id="map" parameterType="map" resultType="blackListVo">
		SELECT *
		FROM BLACKLIST
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="blackListVo">
		UPDATE BLACKLIST
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM BLACKLIST
		WHERE ID = #{id}
	</delete>

</mapper>