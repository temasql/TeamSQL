<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DDL_HISTORY">

	<!-- Insert -->
	<insert id="insert" parameterType="historyVo">
		INSERT INTO DDL_HISTORY
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="historyVo">
		SELECT *
		FROM DDL_HISTORY
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="historyVo">
		SELECT *
		FROM DDL_HISTORY
	</select>
	
	<select id="map" parameterType="map" resultType="historyVo">
		SELECT *
		FROM DDL_HISTORY
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="historyVo">
		UPDATE DDL_HISTORY
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM DDL_HISTORY
		WHERE ID = #{id}
	</delete>

</mapper>