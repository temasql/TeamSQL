<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamCalendar">

	<!-- Insert -->
	<insert id="insert" parameterType="teamCalendarVo">
		INSERT INTO TEAM_CALENDAR
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="teamCalendarVo">
		SELECT *
		FROM TEAM_CALENDAR
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="teamCalendarVo">
		SELECT *
		FROM TEAM_CALENDAR
	</select>
	
	<select id="map" parameterType="map" resultType="teamCalendarVo">
		SELECT *
		FROM TEAM_CALENDAR
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="teamCalendarVo">
		UPDATE TEAM_CALENDAR
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM TEAM_CALENDAR
		WHERE ID = #{id}
	</delete>

</mapper>