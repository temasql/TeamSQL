<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="commonTemplate">

	<!-- Insert -->
	<insert id="insert" parameterType="commonTemplateVo">
		INSERT INTO COMMON_TEMPLATE
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="commonTemplateVo">
		SELECT *
		FROM COMMON_TEMPLATE
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="commonTemplateVo">
		SELECT *
		FROM COMMON_TEMPLATE
	</select>
	
	<select id="map" parameterType="map" resultType="commonTemplateVo">
		SELECT *
		FROM COMMON_TEMPLATE
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="commonTemplateVo">
		UPDATE COMMON_TEMPLATE
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM COMMON_TEMPLATE
		WHERE ID = #{id}
	</delete>

</mapper>