<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamCalendarCategory">

	<!-- Insert -->
	<insert id="insert" parameterType="teamCalendarCategoryVo">
		INSERT INTO TEAM_CALENDAR_CATEGORY
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="teamCalendarCategoryVo">
		SELECT *
		FROM TEAM_CALENDAR_CATEGORY
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="teamCalendarCategoryVo">
		SELECT *
		FROM TEAM_CALENDAR_CATEGORY
	</select>
	
	<select id="map" parameterType="map" resultType="teamCalendarCategoryVo">
		SELECT *
		FROM TEAM_CALENDAR_CATEGORY
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="teamCalendarCategoryVo">
		UPDATE TEAM_CALENDAR_CATEGORY
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM TEAM_CALENDAR_CATEGORY
		WHERE ID = #{id}
	</delete>

</mapper>