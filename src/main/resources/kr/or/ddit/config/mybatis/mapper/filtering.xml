<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="filtering">

	<!-- Insert -->
	<insert id="insert" parameterType="filteringVo">
		INSERT INTO FILTERING
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="filteringVo">
		SELECT *
		FROM FILTERING
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="filteringVo">
		SELECT *
		FROM FILTERING
	</select>
	
	<select id="map" parameterType="map" resultType="filteringVo">
		SELECT *
		FROM FILTERING
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="filteringVo">
		UPDATE FILTERING
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM FILTERING
		WHERE ID = #{id}
	</delete>

</mapper>