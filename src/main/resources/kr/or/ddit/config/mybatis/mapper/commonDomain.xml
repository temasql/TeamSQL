<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="commonDomain">

	<!-- Insert -->
	<insert id="insert" parameterType="commonDomainVo">
		INSERT INTO COMMON_DOMAIN
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="commonDomainVo">
		SELECT *
		FROM COMMON_DOMAIN
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="commonDomainVo">
		SELECT *
		FROM COMMON_DOMAIN
	</select>
	
	<select id="map" parameterType="map" resultType="commonDomainVo">
		SELECT *
		FROM COMMON_DOMAIN
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="commonDomainVo">
		UPDATE COMMON_DOMAIN
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM COMMON_DOMAIN
		WHERE ID = #{id}
	</delete>

</mapper>