<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="quizAnswer">

	<!-- Insert -->
	<insert id="insert" parameterType="quizAnswerVo">
		INSERT INTO QUIZ_ANSWER
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="quizAnswerVo">
		SELECT *
		FROM QUIZ_ANSWER
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="quizAnswerVo">
		SELECT *
		FROM QUIZ_ANSWER
	</select>
	
	<select id="map" parameterType="map" resultType="quizAnswerVo">
		SELECT *
		FROM QUIZ_ANSWER
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="quizAnswerVo">
		UPDATE QUIZ_ANSWER
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM QUIZ_ANSWER
		WHERE ID = #{id}
	</delete>

</mapper>