<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tsFile">

	<!-- Insert -->
	<insert id="insert" parameterType="tsFileVo">
		INSERT INTO TS_FILE
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="tsFileVo">
		SELECT *
		FROM TS_FILE
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="tsFileVo">
		SELECT *
		FROM TS_FILE
	</select>
	
	<select id="map" parameterType="map" resultType="tsFileVo">
		SELECT *
		FROM TS_FILE
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="tsFileVo">
		UPDATE TS_FILE
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM TS_FILE
		WHERE ID = #{id}
	</delete>

</mapper>