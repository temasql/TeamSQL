<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reply">

	<!-- Insert -->
	<insert id="insert" parameterType="replyVo">
		INSERT INTO REPLY
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="replyVo">
		SELECT *
		FROM REPLY
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="replyVo">
		SELECT *
		FROM REPLY
	</select>
	
	<select id="map" parameterType="map" resultType="replyVo">
		SELECT *
		FROM REPLY
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="replyVo">
		UPDATE REPLY
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM REPLY
		WHERE ID = #{id}
	</delete>

</mapper>