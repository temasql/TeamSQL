<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tableLock">

	<!-- Insert -->
	<insert id="insert" parameterType="tableLockVo">
		INSERT INTO TABLE_LOCK
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="tableLockVo">
		SELECT *
		FROM TABLE_LOCK
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="tableLockVo">
		SELECT *
		FROM TABLE_LOCK
	</select>
	
	<select id="map" parameterType="map" resultType="tableLockVo">
		SELECT *
		FROM TABLE_LOCK
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="tableLockVo">
		UPDATE TABLE_LOCK
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM TABLE_LOCK
		WHERE ID = #{id}
	</delete>

</mapper>