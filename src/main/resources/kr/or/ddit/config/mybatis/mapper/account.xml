<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="account">

	<!-- Insert -->
	<insert id="insert" parameterType="accountVo">
		INSERT INTO ACCOUNT
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="accountVo">
		SELECT *
		FROM ACCOUNT
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="accountVo">
		SELECT *
		FROM ACCOUNT
	</select>
	
	<select id="map" parameterType="map" resultType="accountVo">
		SELECT *
		FROM ACCOUNT
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="accountVo">
		UPDATE ACCOUNT
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM ACCOUNT
		WHERE ID = #{id}
	</delete>

</mapper>