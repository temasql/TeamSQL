<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="commons">

	<!-- Insert -->
	<insert id="insert" parameterType="Vo">
		INSERT INTO COMMONS
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="Vo">
		SELECT *
		FROM COMMONS
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="Vo">
		SELECT *
		FROM COMMONS
	</select>
	
	<select id="map" parameterType="map" resultType="Vo">
		SELECT *
		FROM COMMONS
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="Vo">
		UPDATE COMMONS
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM COMMONS
		WHERE ID = #{id}
	</delete>

</mapper>