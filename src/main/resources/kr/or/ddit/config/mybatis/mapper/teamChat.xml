<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamChat">

	<!-- Insert -->
	<insert id="insert" parameterType="teamChatVo">
		INSERT INTO TEAM_CHAT
		(
			...
		)
		VALUES
		(
			...
		)
	</insert>
	
	<!-- SelectOne -->
	<select id="get" parameterType="String" resultType="teamChatVo">
		SELECT *
		FROM TEAM_CHAT
		WHERE ID = #{id}
	</select>
	
	<!-- SelectList -->
	<select id="list" resultType="teamChatVo">
		SELECT *
		FROM TEAM_CHAT
	</select>
	
	<select id="map" parameterType="map" resultType="teamChatVo">
		SELECT *
		FROM TEAM_CHAT
		WHERE ID = #{map1}
		AND .. = #{map2}
		...
	</select>
	
	<!-- update -->
	<update id="update" parameterType="teamChatVo">
		UPDATE TEAM_CHAT
		SET COLUMN = #{..}
		WHERE ID = #{id}
	</update>
	
	<!-- delete -->
	<delete id="delete" parameterType="String">
		DELETE
		FROM TEAM_CHAT
		WHERE ID = #{id}
	</delete>

</mapper>